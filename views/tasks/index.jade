h1 Tasks
- if(docs.length)
	table
		tr
			th Task
			th Action
				each task in docs
					tr
						td #{task.task}
						td 
							a(href="/tasks/" + task.id + "/edit") Edit
- else
	p You have no tasks

a(href="/tasks/new", class="btn primary") Add a Task